{
  "name": "resumedjango-frontend-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm start",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm run start"
  }
}
